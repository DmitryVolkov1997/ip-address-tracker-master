{
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --out-dir=build"
  },
  "devDependencies": {
    "buffer": "^5.7.1",
    "parcel": "^2.8.2"
  },
  "dependencies": {
    "axios": "^1.2.1",
    "babel-polyfill": "^6.26.0",
    "leaflet": "^1.9.3"
  }
}
